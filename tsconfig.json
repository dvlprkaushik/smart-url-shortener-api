{
  "compilerOptions": {
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "module": "nodenext",
    "target": "es2022",

    // module resolution
    "moduleResolution": "nodenext",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,

    // extras
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    // safety
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    //* path mapping
    /*
      💡 Minor cleanup: You don’t actually need all subpaths (@/routes/*, @/controllers/*, etc.),
      because "@/*": ["src/*"] already covers all those.

      But keeping them isn’t wrong — just redundant.
    */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "tsconfig.json"]
}
